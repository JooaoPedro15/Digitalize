{
  "name": "digitalize-legacy-node-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Protótipo Node/Express legado. O backend oficial do Digitalize é Java/Spark.",
  "main": "server.js",
  "scripts": {
    "legacy:start": "node server.js",
    "legacy:dev": "node server.js",
    "legacy:serve:front": "json-server --watch data/unified_db.json --routes routes.json --static ./public --port 3000 --cors"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
