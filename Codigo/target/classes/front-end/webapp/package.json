{
  "name": "digitalize-unified",
  "version": "1.0.0",
  "description": "Sistema unificado Digitalize",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "serve:front": "json-server --watch data/unified_db.json --routes routes.json --static ./public --port 3000 --cors"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}